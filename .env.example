# Copy to .env and fill in. Never commit .env.
OPENAI_API_KEY=

# Model for `python -m rag_lab answer`. Example: gpt-4o-mini.
# The key above is never hard-coded in source.
RAG_LAB_LLM_MODEL=gpt-4o-mini
